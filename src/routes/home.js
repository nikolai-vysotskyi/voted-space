import React from "react";
import Slider from './../components/Slider'

const Home = ({}) => {
    return (
        <div>
            <div className='home_bg'></div>
            <Slider></Slider>
        </div>
    );
};

export default Home;
