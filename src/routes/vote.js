import React from "react";
import Logo from "./Logo";

const Header = ({}) => {
    return (
        <div>
            <Logo />
            <div>

            </div>
        </div>
    );
};

export default Header;
